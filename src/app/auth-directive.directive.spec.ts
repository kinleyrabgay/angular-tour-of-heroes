import { AuthDirectiveDirective } from './auth-directive.directive';

describe('AuthDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
